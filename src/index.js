import React from 'react';
import ReactDOM from 'react-dom/client';
import MultiplicationQuiz from './components/App.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MultiplicationQuiz />
  </React.StrictMode>
);
